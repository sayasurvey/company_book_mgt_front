import { Layout } from '../../components/layout';
import { BookFormTemplate } from '../../components/book_form_template';

const BookRegistrationForm = () => {

  return (
    <Layout>
      <BookFormTemplate />
    </Layout>
  );
}

export default BookRegistrationForm;
